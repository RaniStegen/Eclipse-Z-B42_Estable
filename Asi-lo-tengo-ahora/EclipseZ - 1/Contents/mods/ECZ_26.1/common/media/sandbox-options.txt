VERSION = 1,

option OnWeaponSwing.Tier1dmg
{
    type = double,
    min = 0.1,
    max = 10.0,
    default = 1.0,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier1dmg,
}

option OnWeaponSwing.Tier2dmg
{
    type = double,
    min = 0.1,
    max = 10.0,
    default = 0.85,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier2dmg,
}

option OnWeaponSwing.Tier3dmg
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.7,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier3dmg,
}

option OnWeaponSwing.Tier4dmg
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.55,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier4dmg,
}

option OnWeaponSwing.Tier5dmg
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.4,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier5dmg,
}

option OnWeaponSwing.Tier6dmg
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.25,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier6dmg,
}
	
option OnWeaponSwing.Tier1critrate
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 1.0,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier1critrate,
}

option OnWeaponSwing.Tier2critrate
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.85,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier2critrate,
}

option OnWeaponSwing.Tier3critrate
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.7,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier3critrate,
}

option OnWeaponSwing.Tier4critrate
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.55,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier4critrate,
}

option OnWeaponSwing.Tier5critrate
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.4,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier5critrate,
}

option OnWeaponSwing.Tier6critrate
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.25,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier6critrate,
}
	
option OnWeaponSwing.Tier1critmulti
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 1.0,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier1critmulti,
}

option OnWeaponSwing.Tier2critmulti
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.85,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier2critmulti,
}

option OnWeaponSwing.Tier3critmulti
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.7,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier3critmulti,
}

option OnWeaponSwing.Tier4critmulti
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.55,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier4critmulti,
}

option OnWeaponSwing.Tier5critmulti
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.4,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier5critmulti,
}

option OnWeaponSwing.Tier6critmulti
{
    type = double,
    min = 0.1,
    max = 10.0,
	default = 0.25,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_Tier6critmulti,
}

option OnWeaponSwing.RangedDamageMultiplier
{
    type = double,
    min = 1.0,
    max = 3.0,
    default = 1.5,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_RangedDamageMultiplier,
}

option OnWeaponSwing.swingrename
{
	type = boolean,
	default = true,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_swingrename,
}

option OnWeaponSwing.equiprename
{
	type = boolean,
	default = true,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_equiprename,
}

option OnWeaponSwing.basehealth
{
	type = double,
	min = 1.0,
	max = 30.0,
	default = 1.7,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_basehealth,
}

option OnWeaponSwing.basesprinter
{
	type = integer,
	min = 0,
	max = 100,
	default = 1,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_basesprinter,
}

option OnWeaponSwing.basepinpoint
{
	type = integer,
	min = 0,
	max = 100,
	default = 1,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_basepinpoint,
}

option OnWeaponSwing.basecognition
{
	type = integer,
	min = 0,
	max = 100,
	default = 1,

	page = OnWeaponSwing,
	translation = OnWeaponSwing_basecognition,
}