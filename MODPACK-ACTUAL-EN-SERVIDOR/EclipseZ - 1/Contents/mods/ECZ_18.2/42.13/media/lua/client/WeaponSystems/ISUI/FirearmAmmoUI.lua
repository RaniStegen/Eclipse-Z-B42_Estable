require("WeaponSystems/ISUI/AmmoLoaderUI")

-----------------------------------------------------------
-- FirearmAmmoUI  (backward-compat stub)
-- All functionality is now in AmmoLoaderUI.
-----------------------------------------------------------
FirearmAmmoUI = {}

function FirearmAmmoUI.OpenPanel(player)
    AmmoLoaderUI.OpenPanel(player)
end

function FirearmAmmoUI.TogglePanel(player)
    AmmoLoaderUI.TogglePanel(player)
end
