# Dependencias externas del servidor

Los siguientes elementos forman parte de la configuración del servidor, pero no del contenido interno de este repositorio:

- Workshop Item `3755715727` — mod `NewMusic`.
- Workshop Item `3739256725` — mod `eclipsemusic`.

Se mantienen en `WorkshopItems=` y `Mods=`. Los auditores los reconocen como externos y no exigen que sus carpetas o `mod.info` estén dentro de `MODPACK-ACTUAL-EN-SERVIDOR`.

La revisión interna del modpack queda completa con **76 IDs propios** y **2 dependencias externas declaradas**.
